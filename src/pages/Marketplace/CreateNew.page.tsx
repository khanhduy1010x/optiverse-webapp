import React from 'react';

const MarketplaceCreateNewPage: React.FC = () => {
    return (
        <div className="p-6">
            <h1 className="text-2xl font-semibold">Tạo mới gói/ứng dụng</h1>
            <p className="text-gray-600 mt-2">UI mẫu tạo mới trong Marketplace.</p>
            <div className="mt-6 rounded-lg border border-dashed border-gray-300 p-6">
                <p className="text-gray-500">Form mẫu tạo mới (placeholder).</p>
            </div>
        </div>
    );
};

export default MarketplaceCreateNewPage;
